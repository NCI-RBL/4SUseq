{
    "__default__": {
        "gres": "lscratch:32",
        "mem": "20g",
        "partition": "norm,ccr",
        "threads": "2",
        "time": "1:00:00",
        "name"      : "{rule}.{wildcards}",
        "output"    : "logs/${{SLURM_JOBID}}.{jobid}.{rule}.{wildcards}.out",
        "error"     : "logs/${{SLURM_JOBID}}.{jobid}.{rule}.{wildcards}.err"
    }
}
